online:
  as_of: "2025-01-01T00:00:00+00:00"   # injected clock; no module reads OS time
  top_k: 100                           # validator-fixed top-100 cut
  malformed_record_policy: skip        # skip | abort
  score_presentation:
    transform: identity
    decimals: 6
    floor_sentinel: 0.0                # gated candidates floor below any genuine one
