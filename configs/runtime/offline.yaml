# Layer 1 (offline) — O0..O18 build parameters.
determinism:
  seed: 20240601

offline:
  as_of: "2025-01-01T00:00:00+00:00"   # injected clock; no module reads OS time
  seed: 20240601
  st_model_id: "sentence-transformers/all-MiniLM-L6-v2"
  st_model_revision: "fa97f6e7cb1a59073dff9e6b13e2715cf7475ac9"  # pinned
  kmeans_k: 24
  search_budget: 2000
  embedding_batch_size: 256
  feature_batch_size: 4096
