# O9 weight-search SEED (candidate priors), not the online contract.
# Keys are score-component identifiers, cross-checked against domain.ScoreComponent
# when O9 emits artifacts/weights/scoring_weights.locked.yaml. Editing this file
# changes the search starting point, never the frozen online weights.
layout_version: "1.0.0"
weights:
  semantic_fit: 0.30
  competency: 0.25
  career: 0.20
  credibility: 0.10
  logistics: 0.08
  eligibility: 0.07


