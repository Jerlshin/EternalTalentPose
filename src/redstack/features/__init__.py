"""Features layer — pure structured feature extraction.

Owner layer: features.
Allowed imports: domain, config.schema, stdlib, numpy.

Sprint 0 placeholder: package marker. Re-export surface is curated as
modules land; no implementation is bound here yet.
"""
from __future__ import annotations

__all__: tuple[str, ...] = ()
