"""Adapters layer — infrastructure; impure; one port each.

Owner layer: adapters.
Allowed imports: domain, ports, config.schema, infrastructure libs.

Sprint 0 placeholder: package marker. Re-export surface is curated as
modules land; no implementation is bound here yet.
"""
from __future__ import annotations

__all__: tuple[str, ...] = ()
