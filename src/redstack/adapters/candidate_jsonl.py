"""JsonlCandidateSourceAdapter implements CandidateSourcePort. Gzip-transparent, UTF-8 strict, O(1) memory, Malformed as data.

Owner layer: adapters.
Allowed imports: ports, domain, io, gzip, json.

Sprint 0 placeholder: declarations only, no implementation.
"""
from __future__ import annotations

__all__: tuple[str, ...] = (
    "JsonlCandidateSourceAdapter",
)
