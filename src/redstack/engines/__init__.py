"""Engines layer — the 11 stateless domain services.

Owner layer: engines.
Allowed imports: domain, ports, features, config.schema.

Sprint 0 placeholder: package marker. Re-export surface is curated as
modules land; no implementation is bound here yet.
"""
from __future__ import annotations

__all__: tuple[str, ...] = ()
