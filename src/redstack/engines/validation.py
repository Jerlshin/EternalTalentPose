"""ValidationEngine — validator rules as code + Stage-4 reasoning checks; defence-in-depth at R9.

Owner layer: engines.
Allowed imports: domain, ports (injected), features, config.schema.

Sprint 0 placeholder: declarations only, no implementation.
"""
from __future__ import annotations

__all__: tuple[str, ...] = (
    "ValidationEngine",
)
