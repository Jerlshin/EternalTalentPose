"""redstack rank -> runs R0..R9 -> writes submission.csv + run_report.json. The spec 10.3 single reproduce command.

Owner layer: cli.
Allowed imports: pipelines.online, config.

Sprint 0 placeholder: declarations only, no implementation.
"""
from __future__ import annotations

__all__: tuple[str, ...] = (
    "rank",
)
