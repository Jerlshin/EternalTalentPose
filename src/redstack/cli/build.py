"""redstack build -> runs O0..O18 -> writes artifacts/ + MANIFEST.json.

Owner layer: cli.
Allowed imports: pipelines.offline, config.

Sprint 0 placeholder: declarations only, no implementation.
"""
from __future__ import annotations

__all__: tuple[str, ...] = (
    "build",
)
